<h1>Tracks on <%= link_to "#{@album.name}", band_url(@album) %></h1>
	<% @album.tracks.each do |track| %>
	  <h3><%= link_to "#{track.name}", track_url(track) %></h3>
	<% end %>
	
<%= link_to "Create a New Track", new_album_track_url %>